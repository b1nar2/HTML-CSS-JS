for

foreach

for ... in

for ... of

-조건문-
if(조건) {
			조건이 참일 때 실행할 코드
}

if()







const
- 상수 선언에는 대소문자 모두 사용할 수 있지만, 일반적인 관습은 모두 대문자를 사용하는 것.


let
- 재선언 불가능. 변수값 재할당 가능

var
- 재선언 가능.


UI에 애니메이션 추가는 CSS로 하는 게 좋음 => 성능때문
one-wqy 애니메이션 => a->b 로 변하는 것. a.시작스타일, b최종스타일-

display: none;와 visibility: hidden; 차이 => 유사하나 none은 없애는 것, hidden은 숨기는 것-

버튼: 전송버튼 type="submit" 그 외 버튼 "button"

중괄호 뒤 ; 생략가능. 소괄호 뒤 ; 생략불가

js 변수에 저장된 키를 통해 객체의 속성에 접근할 때 대괄호 표기법 사용.
문자열이 아닌 변수 이름에는 따움표 표기 사용하면 안 됨.


DAO 객체

CRUD methods
 =>	create(insert) reae(select) 
	Update(update)/Delete(delete)
	
	record(tuple, row)
	DML (Data Maniplation Language)



parent class <---->  child class(type)
super class <----> sub class (type)
base class <---->  derived(파생) class(type)

